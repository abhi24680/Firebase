/**
 * Firebase Configuration
 * 
 * IMPORTANT: Replace the placeholder values below with your actual Firebase 
 * project credentials found in the Firebase Console (Project Settings > General).
 * 
 * You can get these by:
 * 1. Going to https://console.firebase.google.com/
 * 2. Selecting your project.
 * 3. Clicking the gear icon (Settings) > Project Settings.
 * 4. Under "Your apps", find your Web App and copy the 'firebaseConfig' object.
 */
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY",
  authDomain: "REPLACE_WITH_YOUR_AUTH_DOMAIN",
  projectId: "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket: "REPLACE_WITH_YOUR_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
  appId: "REPLACE_WITH_YOUR_APP_ID"
};
